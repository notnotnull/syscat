name: Clang Format

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
    
  clang_format:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: clang-format
        run: make checklint
